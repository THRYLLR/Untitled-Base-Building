//This file is used as a basic template for an interface

/*class MetaInterface {
  constructor(...args) {
    doSomethingWith(args);
    this.manager = new MetaManager(); 
      //Managers should only be instantiated once, 
      //so new MetaManager() should just return the already created mutable instance of MetaManager.
  }

  //Implement more functions here
  interfaceFunction(...args) {
    this.manager.someFunction(this.someProperty)
  }
}*/
